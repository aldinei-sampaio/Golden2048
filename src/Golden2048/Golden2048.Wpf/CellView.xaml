<UserControl 
    x:Class="Golden2048.Wpf.CellView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:local="clr-namespace:Golden2048.Wpf"
    mc:Ignorable="d" 
    d:DesignHeight="100" d:DesignWidth="100">

    <Image 
        Canvas.Left="{Binding Left, NotifyOnTargetUpdated=True}"
        Canvas.Top="{Binding Top}" 
        Source="Images\2.png" 
        Width="100"
        Name="img"
        Binding.TargetUpdated="img_TargetUpdated"
        >
        <Image.Triggers>
            <EventTrigger RoutedEvent="Binding.SourceUpdated">
                <BeginStoryboard>
                    <Storyboard Name="stb">
                        <DoubleAnimation 
                            Name="anm"
                            Duration="0:0:0.25"
                            Storyboard.Target="{Binding ElementName=img}"
                            Storyboard.TargetProperty="(Canvas.Left)"
                            AutoReverse="False"
                            />
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Image.Triggers>
    </Image>

</UserControl>
